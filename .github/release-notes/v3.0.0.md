重构保险库，完善数据加密与解锁机制

优化 SFTP 文件管理与传输体验

改进终端页签和界面交互，修复若干问题

同版本修复：修正首页缓存策略，为 SSH/SFTP 的 WASM 和 Go 运行脚本添加内容哈希地址，避免升级后新旧资源混用，构建时同步配套运行脚本

已访问旧版本的用户，更新后请强制刷新一次页面，清除旧版首页缓存，本次修复版本号仍为 3.0.0

注意：3.0.0 不兼容旧版数据库，请在升级前导出服务器配置，创建新保险库后导入，设置和标签需重新配置，导出文件含明文密码，请妥善保管
